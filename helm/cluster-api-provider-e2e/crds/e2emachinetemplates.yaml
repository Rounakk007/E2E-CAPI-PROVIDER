apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: e2emachinetemplates.infrastructure.e2enetworks.com
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
spec:
  group: infrastructure.e2enetworks.com
  names:
    kind: E2EMachineTemplate
    listKind: E2EMachineTemplateList
    plural: e2emachinetemplates
    singular: e2emachinetemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: E2EMachineTemplate is the Schema for the e2emachinetemplates API.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: E2EMachineTemplateSpec defines the desired state of E2EMachineTemplate.
            properties:
              template:
                description: Template contains the E2EMachine spec to stamp out.
                properties:
                  metadata:
                    description: Standard object metadata.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  spec:
                    description: Spec is the E2EMachineSpec to use for new machines.
                    properties:
                      enableBackup:
                        type: boolean
                      enableIPv6:
                        type: boolean
                      enablePublicIP:
                        type: boolean
                      image:
                        type: string
                      location:
                        type: string
                      plan:
                        type: string
                      providerID:
                        type: string
                      region:
                        type: string
                      securityGroupIDs:
                        items:
                          type: integer
                        type: array
                      sshKeys:
                        items:
                          type: string
                        type: array
                      vpcID:
                        type: string
                    required:
                    - image
                    - plan
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
