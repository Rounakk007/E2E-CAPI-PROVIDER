apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: e2eclustertemplates.infrastructure.e2enetworks.com
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
spec:
  group: infrastructure.e2enetworks.com
  names:
    kind: E2EClusterTemplate
    listKind: E2EClusterTemplateList
    plural: e2eclustertemplates
    singular: e2eclustertemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: E2EClusterTemplate is the Schema for the e2eclustertemplates API.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: E2EClusterTemplateSpec defines the desired state of E2EClusterTemplate.
            properties:
              template:
                description: Template contains the E2ECluster spec to stamp out.
                properties:
                  metadata:
                    description: Standard object metadata.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  spec:
                    description: Spec is the E2EClusterSpec to use for new clusters.
                    properties:
                      controlPlaneEndpoint:
                        properties:
                          host:
                            type: string
                          port:
                            format: int32
                            type: integer
                        required:
                        - host
                        - port
                        type: object
                      loadBalancer:
                        properties:
                          id:
                            type: integer
                          name:
                            type: string
                          securityGroupID:
                            type: integer
                        type: object
                      location:
                        type: string
                      network:
                        properties:
                          vpcID:
                            type: string
                        type: object
                      region:
                        type: string
                    required:
                    - region
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
